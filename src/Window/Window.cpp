#include "Window.hpp"

Window::Window(const int width, const int height, const int fpsMax, const std::string title)
{
    // sf::VideoMode 
    // _window = std::make_shared<sf::RenderWindow>(sf::VideoMode(width, height), title);
    // _window->setFramerateLimit(fpsMax);
}