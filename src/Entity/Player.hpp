/*
** EPITECH PROJECT, 2023
** B-rtype
** File description:
** Player
*/

#pragma once

#include <bits/stdc++.h>
#include "Entity.hpp"

class Player : public Entity {
    public:
        Player(u_int64_t id) : Entity(id) {};
        ~Player() = default;

    protected:
    private:
};
